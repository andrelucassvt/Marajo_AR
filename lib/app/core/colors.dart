import 'package:flutter/material.dart';

MaterialColor primaryColor = Colors.red;

//flutter pub run intl_utils:generate --no-sound-null-safety
//flutter pub run build_runner build --delete-conflicting-outputs 